# v0.3 Speech — Live Path Validation Evidence (issue #177)

Date: 2026-08-11
Base: `origin/main` @ `40a86f3` (all of #157-#163 merged)
Environment:
- Chrome 151.0.7922.108 (real headed Chrome via Playwright persistent context)
- Extension build: current `pnpm build` output (dist/)
- Twitch: live channel `tororo_vtuber` (also probed xqc/lvndmark etc.)
- Gemini model: `gemini-2.5-flash`, key from `GEMINI_API_KEY` (BYOK)

## Automatable gates (all PASS)

| Gate | Result |
| --- | --- |
| `pnpm check:agent-docs` | PASS |
| `pnpm lint` | 0 errors (2 pre-existing warnings) |
| `pnpm typecheck` | PASS |
| `pnpm test` | 1198 passed |
| `pnpm build` | PASS |
| `pnpm test:e2e` | 16/16 passed (real Chromium) |
| Packaged extension | manifest has `tabCapture`/`offscreen`/`activeTab`, `minimum_chrome_version: 116`, offscreen index.html + worklet + speech modules in dist |

## Live path stages (real Chrome, headed)

| Stage | Result | Evidence |
| --- | --- | --- |
| Extension SW loads in real Chrome | PASS | `tachi-lens installed`; extId `cclfofidpiibebfoldhodjiemnhaonkh` |
| Twitch live channel loads | PASS | `video` element present on twitch.tv/tororo_vtuber |
| First-enable consent flow | PASS | Toggle → consent panel ("Enable and start") → confirm persisted `speechConsentGranted: true` + `speechEnabled: true` in chrome.storage.local |
| SW receives `speech_control` | PASS | `[SW-RECV] speech_control` observed when popup toggle fires |
| `getMediaStreamId` succeeds from gesture path | **UNVERIFIED (needs human action-click)** | `getMediaStreamId` rejects with `"Extension has not been invoked for the current page (see activeTab permission)"` from automation (no genuine invocation); a human action-click is required. When the popup is the focused window, `tabs.query({active:true})` also returns the popup tab, so `resolveTwitchTab` finds no active Twitch tab (known popup-context limitation) |
| Offscreen capture starts/stops | **UNVERIFIED** | Blocked by the same gesture gate; offscreen count stayed 0 under automation |
| Twitch audio remains audible | **UNVERIFIED** | Requires an active capture stream; cannot be observed until the gesture gate passes |
| Real PCM/WAV accepted by Gemini | **PASS (D2)** | `toWav` WAV + `audio/wav` inlineData → `generateContent` on `gemini-2.5-flash` → HTTP 200 with valid `{"text":"[LONG BEEP]","translatedText":"[長嗶聲]"}` |
| Transcription + translation reach the overlay | **PASS (D3, real Twitch page + real runtime messages)** | Overlay mounted on `speech_state capturing`; interim `"Hello there viewers"` → final `"大家好"` replaced in place (interim 0.55 opacity, final 1.0); error chip showed only the sanitized key `"Speech authentication failed. Please check your speech API key"` |
| Disable/navigation/error stops capture | PARTIAL — disable path PASS (state→idle destroys overlay, unit+e2e); capture-stop itself UNVERIFIED (blocked by gesture gate) |
| No raw audio / API-key persistence or leakage | PASS | storage keys after flow: `speechProviderApiKeys`, `userSettings` only; no audio blobs; SW console clean of key/audio leak; `speech_state`/`speech_caption` payloads carry only fixed errorKey / display text |
| Latency / provider cost | UNVERIFIED | Requires a live transcription stream (blocked by gesture gate) |

## D1/D2/D3 verdicts

- **D1** — resolved (SW-originated, gesture-gated call site) but the real-Chrome streamId→offscreen path is **UNVERIFIED**: automation cannot synthesize a genuine action-click. Tracked in issue #177; a human must click the extension action and toggle.
- **D2** — **PASS**: WAV container + `audio/wav` accepted by real Gemini `gemini-2.5-flash`.
- **D3** — **PASS (UX surface)**: interim/final caption replacement verified in a real Twitch page with real runtime messages; live-STT latency remains to measure on the gesture path.

## Residual production risk

The gesture path is the last unknown. Chrome fail-closes capture without a real invocation, which
is the intended security behavior, but it means the offscreen capture + audio-mixing + live-STT
chain has not yet been observed in a real browser. No production change is warranted from this
validation round; the remaining gap is a manual human action-click verification.
